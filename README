This is a simple jython-servlet application that modifies a stack (push and pop) and email's the stack contents.

Stack is currently configured against an SQL database.

Constants need to be set for each .py file.
